<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/select_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Day"
        android:textSize="20sp"
        android:textColor="@color/black"
        android:paddingLeft="25dp"
        android:paddingTop="25dp"
        android:paddingBottom="25dp"
        android:background="@color/orange" />

    <TextView
        android:id="@+id/descrip_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select day for your task"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:paddingLeft="25dp"
        android:paddingTop="25dp"
        android:paddingBottom="25dp"
        android:layout_below="@+id/select_textview"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_day"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/descrip_textview"
        tools:listitem="@layout/day_dialog_item"
        android:layout_marginBottom="20dp">
    </android.support.v7.widget.RecyclerView>

</RelativeLayout>